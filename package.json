{
  "dependencies": {
    "esbuild": "^0.25.2"
  },
  "scripts": {
    "build": "esbuild payload.template.ts --bundle --minify --global-name=\"\" --outfile=out/payload.template.js",
    "dev": "esbuild payload.template.ts --bundle --minify --global-name=\"\" --outfile=out/payload.template.js && python server.py"
  }
}